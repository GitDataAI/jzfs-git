use crate::App;

impl App {
    // pub async fn service_repo_git_branch_list(&self, repo_uid: Uuid) -> AppResult<Vec<GitBranchModel>> {
    //     return self.repo_git_branch_list(repo_uid).await;
    // }
}